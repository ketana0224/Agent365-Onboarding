# Microsoft Agent Framework (MAF) — ホスト型エージェント本体（B と同一）
agent-framework-core
# APIM 経由で Azure OpenAI 互換 (Chat Completions) を叩くため OpenAIChatCompletionClient を使う。
agent-framework-openai
agent-framework-foundry
# mcp は --pre 下だと alpha が選ばれ raw client API が変わる恐れがあるため固定
mcp==1.28.0
aiohttp>=3.10.0

# サイドカー（Entra SDK for AgentID）との HTTP 通信
httpx>=0.27.0
# AccessToken 型（SidecarCredential が返す）
azure-core>=1.30.0

# 認証（UAMI 切り戻し用）・可観測性
azure-identity>=1.19.0
azure-monitor-opentelemetry>=1.6.0

# Web サーバー（ACA 外部 Ingress）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# .env 読み込み（ローカル実行用）
python-dotenv>=1.0.0
