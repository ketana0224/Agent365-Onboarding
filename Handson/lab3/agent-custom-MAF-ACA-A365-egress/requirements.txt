# Microsoft Agent Framework (MAF) — ホスト型エージェント本体
# core（agent_framework 名前空間 + @tool）+ Foundry プロバイダー + MCP クライアント
# 注: meta パッケージ agent-framework は core[all] を引き込み、
#     hyperlight-sandbox-backend-wasm の依存解決に失敗するため core を直接指定する。
agent-framework-core
# APIM (apim-aigateway-eastus2) 経由で Azure OpenAI 互換エンドポイント (Chat Completions) を
# 叩くため OpenAIChatCompletionClient を使う。agent-framework-foundry は切り戻し用に残す。
agent-framework-openai
agent-framework-foundry
# mcp は --pre 下だと 2.0.0a2 (alpha) が選ばれ raw client API が変わる恐れがあるため固定
mcp==1.28.0
aiohttp>=3.10.0
# Agent ID 出口化（fmi_path 2 ステップ交換）の token 取得に使用
httpx>=0.27.0

# 認証・可観測性（B と同じ App Insights へ接続）
azure-identity>=1.19.0
azure-monitor-opentelemetry>=1.6.0

# Web サーバー（ACA 外部 Ingress）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# .env 読み込み（ローカル実行用）
python-dotenv>=1.0.0
