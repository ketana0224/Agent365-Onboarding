# Microsoft Agent Framework (MAF) — ホスト型エージェント本体
# 重要: host ラッパー azure-ai-agentserver-agentframework==1.0.0b17 は
#   agent-framework-core>=1.0.0rc2,<=1.0.0rc3 / agent-framework-azure-ai>=1.0.0rc2,<=1.0.0rc3
#   に固定されている。一方 agent-framework-foundry(FoundryChatClient) は core>=1.9.0 を要求し
#   両立不能（core 1.9 は AgentProtocol を持たず host adapter が ImportError で起動失敗する）。
#   また agent-framework-openai には rc3 が存在しない（最古 rc6）。
#   よって rc3 で唯一整合する組み合わせに固定し、LLM クライアントは agent-framework-azure-ai の
#   AzureOpenAIChatClient を使う（FoundryChatClient/OpenAIChatCompletionClient は使わない）。
agent-framework-core==1.0.0rc3
agent-framework-azure-ai==1.0.0rc3
# Foundry Hosted Agent（responses プロトコル）ホスト ラッパー。
# from_agent_framework(agent).run() が HTTP/ヘルスチェック/POST /responses/履歴管理を担う。
# azure-ai-agentserver-core==1.0.0b17 を依存として自動導入する。
azure-ai-agentserver-agentframework==1.0.0b17
# mcp は --pre 下だと 2.0.0a2 (alpha) が選ばれ raw client API が変わる恐れがあるため固定
mcp==1.28.0
aiohttp>=3.10.0

# 認証・可観測性（プロンプトエージェントと同じ App Insights へ接続）
azure-identity>=1.19.0
azure-monitor-opentelemetry>=1.6.0

# Web サーバー（旧 ACA 版 main.py.aca-backup 用 / 切り戻し参照）。
# responses ホストは azure-ai-agentserver が内蔵するため必須ではない。
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# .env 読み込み（ローカル実行用）
python-dotenv>=1.0.0
